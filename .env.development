PORT=3000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Embedding Provider: 'local' | 'google' | 'openai'
# - local: HuggingFace 384-dim (offline, no API key)
# - openai/google: Cloud embeddings (requires API key)
EMBEDDING_PROVIDER=local

# LLM Provider: 'google' | 'openai'
# - Always cloud-based (no local option yet)
# - Powers Sunita persona responses
LLM_PROVIDER=google

# API Keys (required based on provider selection)
GOOGLE_API_KEY=sua_chave_aqui
OPENAI_API_KEY=sua_chave_aqui
